# This file contains the auditctl rules that are loaded
# whenever the audit daemon is started via the initscripts.
# The rules are simply the parameters that would be passed
# to auditctl.

# First rule - delete all
-D

# Increase the buffers to survive stress events.
# Make this bigger for busy systems
-b 320

# Feel free to add below this line. See auditctl man page

-w /etc/group -p wa -k identity
-w /etc/passwd -p wa -k identity
-w /etc/gshadow -p wa -k identity
-w /etc/shadow -p wa -k identity
-w /etc/security/opasswd -p wa -k identity
-w /etc/sudoers -p wa -k identity
-w /etc/audit/audit.rules -p wa -k auditing
-w /etc/audit/auditd.conf -p wa -k auditing
-w /etc/libaudit.conf -p wa -k auditing
-w /etc/sysconfig/auditd -p wa -k auditing
-w /etc/login.defs -p wa -k auditing
-w /etc/security/limits.conf -p wa -k auditing
-w /etc/ssh/sshd_config -p wa -k auditing
-w /etc/rsyslog.conf -p wa -k auditing
-w /etc/cron.allow -p wa -k auditing
-w /etc/cron.deny -p wa -k auditing
-w /etc/at.allow -p wa -k auditing
-w /etc/at.deny -p wa -k auditing
-w /etc/cron.hourly/ -p wa -k auditing
-w /etc/cron.daily/ -p wa -k auditing
-w /etc/cron.weekly/ -p wa -k auditing
-w /etc/cron.monthly/ -p wa -k auditing
-w /etc/cron.d/ -p wa -k auditing
-w /etc/crontab -p wa -k auditing
